# Exact versions used in the recorded publication re-execution (Python 3.13.5).
# This is a reproduction pin set, not a claim that these are the newest versions.
numpy==2.3.5
scipy==1.18.1
pandas==2.2.3
